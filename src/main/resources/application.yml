server:
  port: 8081
  
spring:
  application:
    name: devops-test

  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  h2:
    console:
      enabled: true
      
userDefine:
  message: webhook test2